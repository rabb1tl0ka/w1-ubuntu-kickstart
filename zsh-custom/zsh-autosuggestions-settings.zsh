ZSH_AUTOSUGGEST_HIGHLIGHT_STYLE='fg=8'
# Bind 'W' (uppercase) to accept the autosuggestion
bindkey 'W' autosuggest-accept

# Bind 'Ctrl + W' to execute the autosuggestion
bindkey '^W' autosuggest-execute
